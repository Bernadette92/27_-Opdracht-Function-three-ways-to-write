/*function square(number1, number2) {
  const multi = number1 * number1;
  const multi2 = number2 * number2;
  const add = multi + multi2;
  const multi3 = add * add;
  return multi3;
}

console.log(square(5, 6));*/

/* const square = function (number1, number2) {
  const multi = number1 * number1;
  const multi2 = number2 * number2;
  const add = multi + multi2;
  const multi3 = add * add;
  return multi3;
};

console.log(square(5, 6)); */

const square = (number1, number2) => {
  const multi = number1 * number1;
  const multi2 = number2 * number2;
  const add = multi + multi2;
  const multi3 = add * add;
  return multi3;
};

console.log(square(5, 6));
